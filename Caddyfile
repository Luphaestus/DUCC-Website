{
    # Global options
}

{$DOMAIN_NAME:localhost}, {$SERVER_IP:localhost} {
    # Reverse proxy to the app service on port 3000
    reverse_proxy app:3000
    
    # Enable compression
    encode gzip
}
